# external open-source reference clones (own .git history, ~171MB) -- reference
# only, never a copy-paste target; do not commit into this repo
references/

# local Claude Code hooks / settings
.claude/

# scratchpad: local demo runners, offline probes, videos -- machine-local scaffolding,
# not part of the repo (the demo's substance lives in launch/, scripts/, legged_gazebo/)
scratchpad/
